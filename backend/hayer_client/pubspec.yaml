name: hayer_client
description: Starting point for a Serverpod client.

environment:
  sdk: ^3.13.0

resolution: workspace

dependencies:
  serverpod_auth_idp_client: 4.0.0
  serverpod_client: 4.0.0

executables:
  hayer_public_gateway_canary: public_gateway_canary
